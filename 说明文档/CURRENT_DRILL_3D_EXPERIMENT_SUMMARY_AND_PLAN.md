# 钻孔三维分类项目：当前实验总结与后续计划

> 更新时间：2026-08-11

## 1. 当前研究问题

项目目标是使用 3D ResNet18 对钻孔体积进行二分类：

```text
normal    = 0
defective = 1
```

当前研究计划应先说明主干网络的选择，再固定主干研究头部裁剪和结构改动：

1. 在完整体积、统一方向和相同五折协议下比较候选 3D 主干；
2. 固定选定主干，验证头部体积裁剪是否优于完整体积输入；
3. 比较头部裁剪深度长度，不只使用 Head40；
4. 在固定头部长度后，比较 Stem 在 D 方向的浅层卷积核长度 3、5、7；
5. 在已经确定的 Head40-733 模型中验证局部方向性卷积是否有额外收益。

## 2. 数据和统一流程

### 2.1 数据规模

- 训练/验证池：22 块板，共 4,364 个样本。
- 正常样本：3,181 个；缺陷样本：1,183 个。
- 外部锁定测试集：3 块板，共 778 个样本。
- 外部测试集：正常 556 个，缺陷 222 个。
- 训练/验证与外部测试之间没有板号、SampleID 或 RAW SHA256 重叠。

### 2.2 固定五折划分

- 所有主要对比使用同一套 22 板五折清单。
- 五折随机种子：`42、123、2026、3407、777`。
- 五折只划分训练/验证池，外部三板不参与训练、验证、阈值选择或模型选择。
- 验证折样本数：`872、873、874、873、872`。
- 论文主要报告 `best_loss` 检查点。

### 2.3 Head40 裁剪和方向统一

Head40 使用 TOML `center2` 和 `bHeadUp` 生成，深度长度固定为 40：

```text
toml_sequence = file_sequence - 1
center_index  = floor(center2 + 0.5)

bHeadUp=false: [center_index-15, center_index+25)
bHeadUp=true : [center_index-25, center_index+15)
```

方向统一规则为：

```text
bHeadUp=false: 保持 D 轴顺序
bHeadUp=true : 沿 D 轴翻转
目标方向：钻头位于 D 轴高索引侧
```

方向统一在数据加载阶段、增强和归一化之前完成，不修改原始 RAW 文件。

公共数据流程保持一致：

- 每个样本独立进行 P1/P99 截断并归一化到 `[0, 1]`；
- 只做横截面、灰度等轻量增强，不翻转钻孔深度方向；
- 不把 `29×29、37×37、49×49` 横截面插值到同一尺寸；
- 使用按形状组批的数据加载方式。

## 3. 当前模型结构

### 3.1 FullVolume 原始体积 ResNet18

完整体积模型使用标准 3D ResNet18 主体：

```text
Conv3d 1→64，kernel=7×3×3，stride=(2,1,1)
MaxPool3d，stride=(2,1,1)
Layer1/2/3/4 = [2,2,2,2]
通道 = 64→128→256→512
Layer2、Layer3、Layer4 首个残差块 stride=2
AdaptiveAvgPool3d(1) → Dropout(0.5) → Linear(512→2)
```

### 3.2 Head40 基础结构

Head40 模型为了避免在深度 40 的头部体积上过早压缩，使用：

```text
Stem Conv3d：1→64，kernel=7×3×3，stride=(1,1,1)
MaxPool3d：3×3×3，stride=(1,1,1)
Layer1：2 个 BasicBlock，stride=1
Layer2：64→128，首块 stride=2
Layer3：128→256，首块 stride=2
Layer4：256→512，首块 stride=2
AdaptiveAvgPool3d(1) → Dropout(0.5) → Linear(512→2)
```

三种横截面下的最终特征图完全一致：

| 输入体积 | Layer4 最终特征图 |
|---|---|
| `1×40×29×29` | `512×5×4×4` |
| `1×40×37×37` | `512×5×5×5` |
| `1×40×49×49` | `512×5×7×7` |

这里需要区分两个概念：`Head40` 中的 40 是输入体积的 D 方向裁剪长度；`Stem333/533/733` 中的 3、5、7 是 Stem 卷积核的 D 方向长度。前者是头部体积长度消融，后者是网络浅层感受野消融，两组实验不能混为一谈。

### 3.3 ACB 实验结构

ACB 实验只修改 `layer1[0].conv1`，其余结构与 Head40-733 完全一致：

```text
3×3×3 Conv + BN
1×3×3 Conv + BN
3×1×3 Conv + BN
3×3×1 Conv + BN
        ↓
四个分支相加 → BasicBlock 原有 ReLU
```

`layer1[0].conv2`、Layer1 第二个 Block、Layer2/3/4、池化和分类头均未改变。ACB 当前没有实现部署阶段的卷积分支融合。

## 4. 已完成实验结果

下面表格统一使用 `best_loss`。验证集中的 `loss` 是 4,364 个 OOF 样本的汇总值，其余验证指标是五折均值±标准差；外部测试指标全部是三板测试的五折均值±标准差。

### 4.1 验证集

| 模型 | OOF loss | Accuracy | Precision | Recall | F1 | Specificity | AUC | AP |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| FullVolume | 0.167286 | 93.51±1.10% | 90.71±2.56% | 84.78±1.93% | 87.64±2.07% | 96.76±0.92% | 97.46±0.56% | 95.26±0.99% |
| Head40-333 | 0.153877 | 93.74±0.93% | 89.52±2.09% | 87.15±1.84% | 88.31±1.70% | 96.20±0.82% | 97.89±0.44% | 95.97±0.70% |
| Head40-533 | **0.151584** | 94.06±0.87% | 90.23±1.26% | 87.57±2.21% | **88.88±1.70%** | 96.48±0.43% | 97.88±0.32% | 96.03±0.63% |
| Head40-733 | 0.152872 | **94.13±1.09%** | **91.58±2.36%** | 86.31±2.04% | 88.86±2.06% | **97.04±0.85%** | 97.86±0.36% | 95.99±0.56% |
| Head40-733 + ACB | 0.154057 | 93.97±1.01% | 91.00±1.75% | 86.31±2.56% | 88.58±1.97% | 96.82±0.62% | 97.77±0.25% | 95.90±0.49% |

### 4.2 锁定三板测试集

| 模型 | Accuracy | Precision | Recall | F1 | Specificity | AUC | AP |
|---|---:|---:|---:|---:|---:|---:|---:|
| FullVolume | 92.29±0.86% | 86.53±2.78% | 86.58±2.41% | 86.50±1.37% | 94.57±1.43% | 97.71±0.24% | 95.02±0.53% |
| Head40-333 | 92.96±0.65% | 84.99±1.93% | **91.53±1.17%** | 88.13±0.98% | 93.53±1.02% | 98.13±0.18% | 95.86±0.37% |
| Head40-533 | 93.14±0.43% | 86.21±1.46% | 90.45±1.64% | 88.26±0.72% | 94.21±0.77% | 98.16±0.10% | 95.98±0.13% |
| Head40-733 | **93.37±0.65%** | **87.23±2.86%** | 90.09±2.25% | **88.58±0.91%** | **94.68±1.51%** | 98.17±0.17% | **96.03±0.23%** |
| Head40-733 + ACB | 93.32±0.68% | 86.93±1.53% | 90.18±2.58% | 88.50±1.26% | 94.57±0.80% | **98.20±0.16%** | 96.03±0.24% |

外部测试的五折概率集成结果可以作为补充，不作为模型选择依据。当前 ACB `best_loss` 集成的总体结果为：Accuracy `93.44%`、F1 `88.64%`、AUC `98.35%`、AP `96.30%`。

## 5. 当前结论

### 5.1 头部裁剪有效

FullVolume 与 Head40-733 使用相同的五折和外部测试协议。Head40-733 相比 FullVolume：

- 验证 OOF loss：`0.167286 → 0.152872`；
- 外部测试五折平均 F1：`86.50% → 88.58%`；
- 外部测试五折平均 Accuracy：`92.29% → 93.37%`。

因此，当前结果支持“基于 TOML center2 和 bHeadUp 的 Head40 裁剪能够提升钻孔缺陷分类效果”。

### 5.2 Stem 长度选择

333、533、733 只改变 Stem 在 D 方向的卷积核长度，其他条件不变：

- 533 的验证 OOF loss 最低，验证 F1 最高，但与 733 非常接近；
- 733 的外部测试 Accuracy、Precision、F1、Specificity 和 AP 综合最好；
- 733 的最终特征图仍与其他 Head40 尺寸相同。

在当前已经完成的 ResNet18 系列内部，综合验证稳定性和外部泛化，暂定主模型为 **Head40-733**。但论文最终对 ResNet18 的选择依据，还需要补充主干模型对比实验后再定稿。

### 5.3 ACB 结果

ACB 只改 `layer1[0].conv1`，没有超过 Head40-733：

- 验证 OOF loss 高于 733；
- 验证 F1 为 `88.58%`，733 为 `88.86%`；
- 外部测试 F1 为 `88.50%`，733 为 `88.58%`；
- AUC 略高，但提升很小，未转化为 Accuracy 或 F1 提升。

因此暂不继续做其他 ACB 范围或分支变体。ACB 可以作为补充性的负结果消融，也可以在论文篇幅有限时不放入主实验表。

## 6. 已确定的实验计划与状态

| 阶段 | 实验内容 | 状态 | 用途 |
|---|---|---|---|
| 1 | 固定数据、方向统一、22 板五折和 3 板外部测试 | 已完成 | 保证所有模型公平比较 |
| 2 | FullVolume 统一方向下的主干模型对比：ResNet18、ResNet34、DenseNet121、EfficientNet-B0、MobileNetV2 | **待执行** | 说明为什么选择 ResNet18 |
| 3 | FullVolume 原始 3D ResNet18 基线 | 已完成 | 建立完整体积基线 |
| 4 | Head40-733 方向统一 ResNet18 | 已完成 | 验证头部裁剪是否有效 |
| 5 | Head24、Head32、Head40、Head48、Head64 长度消融 | **待执行** | 选择头部体积的 D 方向裁剪长度 |
| 6 | Head40-333、Head40-533、Head40-733 Stem 消融 | 已完成 | 选择 D 方向浅层卷积核长度 |
| 7 | Head40-733 的 Layer1 首个卷积 ACB | 已完成 | 验证局部方向性卷积是否有额外收益 |
| 8 | 确定论文最终主干和主模型 | 待执行 | 先依据主干对比和长度消融，再确定最终模型 |
| 9 | 整理最终论文表格和图 | 待执行 | 统一报告验证集与外部测试集 |

### 6.1 论文建议实验顺序

```text
数据和方向统一说明
        ↓
FullVolume 下的主干模型对比
        ↓
固定主干后：FullVolume vs Head40-733
        ↓
Head24/32/40/48/64 长度消融
        ↓
固定头部长度后：Stem333/533/733 消融
        ↓
确定主干和 Head40-733 主模型
        ↓
外部三板锁定测试
        ↓
ACB 作为可选补充消融
```

### 6.2 目前不再安排的实验

- 长度消融先固定为 Head24、32、40、48、64，完成后不再继续扩展更多长度；
- 不再调整第一层池化，因为当前池化 stride 已经是 1；
- 不再随意扩大或缩小最终特征图；
- 不再重新调数据增强和轻微正则化；
- 不再继续扩展 ACB 到多个 Layer 或多个卷积；
- 主干模型对比只做一轮统一协议比较，不再在多个主干上反复调参；
- 不根据外部测试结果反向更换 checkpoint，统一使用 `best_loss` 规则。

## 7. 最终报告口径

论文主表建议按以下规则：

1. 验证集报告 22 板五折 `best_loss` 的均值±标准差；
2. 外部测试报告 3 板锁定测试的五折均值±标准差；
3. `best_f1` 和 `last` 作为补充，不替代 `best_loss` 主结果；
4. 五折概率集成的 `Board=ALL` 结果作为补充分析，不用于反向选择模型；
5. 在当前 ResNet18 系列中，Head40-733 只是暂定主模型；FullVolume、不同长度、333/533/733 和 ACB 作为对比或消融。
6. 主干对比完成后，再确认 ResNet18 是否作为论文最终主干；不能只凭当前 ResNet18 内部结果声称其优于其他网络。
7. 长度消融必须使用同一 TOML `center2`/`bHeadUp` 规则、同一方向统一方式和同一五折清单；旧的 `head_region_*` 结果不能直接与当前统一方向 Head40 结果混用。

## 8. 结果目录

当前结果保存在：

```text
H:\3d钻孔缺陷分类实验结果_5折交叉验证_多板\train_val\
H:\3d钻孔缺陷分类实验结果_5折交叉验证_多板\test\
```

主要文件：

```text
train_val/<experiment>/oof_metrics_best_loss.csv
train_val/<experiment>/fivefold_mean_std.csv
test/<experiment>_locked_3boards/fivefold_mean_std.csv
test/<experiment>_locked_3boards/ensemble_metrics_by_board.csv
```

本文件是当前项目的总览，不替代各实验自己的模型、训练、测试和说明文件。旧脚本和旧说明保持不变。
